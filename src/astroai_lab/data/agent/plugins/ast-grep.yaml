# ast-grep — Cursor skill for structural search/rewrite.
# Binary (sg): `astroai-lab agent plugins install ast-grep-cli`
id: ast-grep
kind: skill
addon: true
tags:
- lean
- python
summary: Structural code search/rewrite skill (pair with plugin ast-grep-cli for sg)
homepage: https://github.com/ast-grep/agent-skill
agents:
- cursor
install:
  type: github-skill
  repo: ast-grep/agent-skill
  path: ast-grep/skills/ast-grep
