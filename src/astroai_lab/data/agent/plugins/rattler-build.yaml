# rattler-build — legacy addon migrated from addons.json (Phase 3).
# `agent plugins list` / `agent plugins install` surface this via the plugin registry.
id: rattler-build
kind: skill
addon: true
tags:
- science
- python
summary: rattler-build package recipes
homepage: https://github.com/pavelzw/skill-forge
agents:
- cursor
install:
  type: github-skill
  repo: pavelzw/skill-forge
  path: recipes/rattler-build
