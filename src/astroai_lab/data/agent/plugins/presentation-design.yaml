# presentation-design — legacy addon migrated from addons.json (Phase 3).
# `agent plugins list` / `agent plugins install` surface this via the plugin registry.
id: presentation-design
kind: skill
addon: true
tags:
- docs
summary: Presentation / slide design guidance
homepage: https://github.com/pavelzw/skill-forge
agents:
- cursor
install:
  type: github-skill
  repo: pavelzw/skill-forge
  path: recipes/presentation-design
