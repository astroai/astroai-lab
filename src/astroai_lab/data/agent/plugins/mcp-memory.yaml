# mcp-memory — legacy addon migrated from addons.json (Phase 3).
# `agent plugins list` / `agent plugins install` surface this via the plugin registry.
id: mcp-memory
kind: mcp
addon: true
tags:
- lean
summary: Persistent agent memory (already in agent setup)
homepage: ''
default: true
agents:
- cursor
install:
  type: bundled
  note: Included in agent setup MCP configs
