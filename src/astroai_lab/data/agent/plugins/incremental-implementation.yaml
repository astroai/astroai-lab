# incremental-implementation — legacy addon migrated from addons.json (Phase 3).
# `agent addons` / `agent add` surface this via the plugin registry.
id: incremental-implementation
kind: skill
addon: true
tags:
- lean
summary: Thin vertical slices — implement, test, verify, commit
homepage: https://github.com/addyosmani/agent-skills
agents:
- cursor
install:
  type: github-skill
  repo: addyosmani/agent-skills
  path: skills/incremental-implementation
