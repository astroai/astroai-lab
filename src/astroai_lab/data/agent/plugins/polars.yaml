# polars — legacy addon migrated from addons.json (Phase 3).
# `agent plugins list` / `agent plugins install` surface this via the plugin registry.
id: polars
kind: skill
addon: true
tags:
- science
- data
- python
summary: Polars DataFrame workflows for agents
homepage: https://github.com/pavelzw/skill-forge
agents:
- cursor
install:
  type: github-skill
  repo: pavelzw/skill-forge
  path: recipes/polars
